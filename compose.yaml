services:
  web:
    image: web
    build:
      context: .
      dockerfile: src/Web/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ConnectionStrings__Default=Host=postgres;Port=5432;Database=postgres;Username=postgres;Password=postgres;GSS Encryption Mode=Disable
    volumes:
      - dataprotection-keys:/home/app/.aspnet/DataProtection-Keys

  postgres:
    image: postgres:17
    container_name: postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d postgres" ]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
  dataprotection-keys:
